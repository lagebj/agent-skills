# Engineering Agent Skills

Read and follow `AGENTS.md` before modifying this repository.

Skills live under `skills/<name>/SKILL.md`. Use `using-agent-skills` to select and compose them. The repository contains source files only: archive artifacts must not be committed. All changes must pass `python3 scripts/validate-skills.py`.
